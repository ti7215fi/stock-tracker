import { TrendIndicatorDirective } from './trend-indicator.directive';

describe('TrendIndicatorDirective', () => {
  it('should create an instance', () => {
    const directive = new TrendIndicatorDirective();
    expect(directive).toBeTruthy();
  });
});
