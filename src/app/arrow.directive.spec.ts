import { ArrowDirective } from './arrow.directive';

describe('ArrowDirective', () => {
  it('should create an instance', () => {
    const directive = new ArrowDirective();
    expect(directive).toBeTruthy();
  });
});
